classdef SuperBug
    % bug super class
    properties
        Sprite % if we use sprites
        Color % if we use colors/shapes
        PointValue = 5; % how many points the item is worth
        SizeIncrease = 1; % how many units the gecko grows if we do that
    end
end